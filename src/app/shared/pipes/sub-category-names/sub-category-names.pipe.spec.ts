import { SubCategoryNamesPipe } from './sub-category-names.pipe';

describe('SubCategoryNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new SubCategoryNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
